1)npm init
2)npm install --save express
3)npm install --save pug
4)npm install -g nodemon
5)npm install --save express-messages express-session connect-flash express-validator
6)npm install --save passport passport-local bcryptjs
7)npm install --save mongoose
8)npm install --save body-parser   //pour parser la BD
9)npm install --save express-session
10)npm install --save passport
11)npm install --save passport-local
12)npm install connect-mongo --save



//Start Mongo Db
 - mongod --dbpath D:\data\db

//Install RoboMongo.exe

//Bower install
 - Create in racine .bowerrc
 - npm install -g bower
 - bower install bootstrap



/**********************************************BD**********************************************************************/

 - Reservation
 - Appartements


/*
echo "# reservation" >> README.md
git init
git add README.md
git add .
git commit -m "systeme auth/ création user/ envoit du mail"
git remote add origin https://github.com/deadfantom2/reservation.git
git push -u origin master

git push -f origin master
*/

/*
git add .
git commit -m "first commit"
git push -u origin master
*/
*


hello@clementsauvage.me















extends ../layout
block content

    h1 #{title}
    form(method='POST',action='/reservations/edit/'+reservation._id)
      #form-group
        label Etage:
        select(name='etage')
          if reservation.etage == 2
            option(value=reservation.etage, selected='')= reservation.etage
            option(value='3') 3
            option(value='4') 4
          else if reservation.etage == 3
            option(value=reservation.etage, selected='')= reservation.etage
            option(value='2') 2
            option(value='4') 4
          else if reservation.etage == 4
            option(value=reservation.etage, selected='')= reservation.etage
            option(value='2') 2
            option(value='3') 3
      br
      #form-group
        label Type:
        select(name='type')
          if reservation.type == 'Familiale'
            option(value=reservation.type, selected='')= reservation.type
            option(value='Luxe') Luxe
          else if reservation.type == 'Luxe'
            option(value=reservation.type, selected='')= reservation.type
            option(value='Familiale') Familiale

      br
      #form-group
        label Nombre chambre:
          select(name='nbRoom')
            if reservation.nbRoom == 1
              option(value=reservation.nbRoom, selected='')= reservation.nbRoom
              option(value='2') 2
              option(value='3') 3
            else if reservation.nbRoom == 2
              option(value=reservation.nbRoom, selected='')= reservation.nbRoom
              option(value='1') 1
              option(value='3') 3
            else if reservation.nbRoom == 3
              option(value=reservation.nbRoom, selected='')= reservation.nbRoom
              option(value='1') 1
              option(value='2') 2

      br
    input.btn.btn-primary(type='submit',value='Edit')

